@using UnivFI.WebUI.Areas.Admin.ViewModels.Role
@model RoleUpdateViewModel

@{
    ViewData["Title"] = "역할 수정";
    var returnPage = (int)(ViewBag.ReturnPage ?? 1);
    var returnPageSize = (int)(ViewBag.ReturnPageSize ?? 10);
    var returnSearchTerm = (string)(ViewBag.ReturnSearchTerm ?? "");
    var returnSearchFields = (string)(ViewBag.ReturnSearchFields ?? "");
}

<div class="container mx-auto px-4 py-6">
    <div class="max-w-3xl mx-auto">
        <div class="flex items-center mb-6">
            <a asp-action="Index" asp-route-page="@returnPage" asp-route-pageSize="@returnPageSize"
                asp-route-searchTerm="@returnSearchTerm" asp-route-searchFields="@returnSearchFields"
                class="text-blue-600 hover:text-blue-800 transition-colors mr-3">
                <i class="bi bi-arrow-left"></i> 목록으로
            </a>
            <h1 class="text-2xl font-bold">@ViewData["Title"] - @Model.RoleName</h1>
        </div>

        <div class="bg-white rounded-lg shadow-md p-6">
            <form asp-action="Edit" method="post">
                <div asp-validation-summary="ModelOnly" class="text-red-600 mb-4"></div>
                <input type="hidden" asp-for="Id" />
                <input type="hidden" name="returnPage" value="@returnPage" />
                <input type="hidden" name="returnPageSize" value="@returnPageSize" />
                <input type="hidden" name="returnSearchTerm" value="@returnSearchTerm" />
                <input type="hidden" name="returnSearchFields" value="@returnSearchFields" />

                <div class="mb-4">
                    <label asp-for="RoleName" class="block text-gray-700 font-medium mb-2">역할명</label>
                    <input asp-for="RoleName"
                        class="w-full px-4 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-500" />
                    <span asp-validation-for="RoleName" class="text-red-600 text-sm"></span>
                    <p class="text-gray-500 text-sm mt-1">역할 이름을 수정하면 이 역할이 할당된 모든 사용자와 메뉴에 영향을 줍니다.</p>
                </div>

                <div class="mb-4">
                    <label asp-for="RoleComment" class="block text-gray-700 font-medium mb-2">역할 설명</label>
                    <textarea asp-for="RoleComment" rows="3"
                        class="w-full px-4 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-500"></textarea>
                    <span asp-validation-for="RoleComment" class="text-red-600 text-sm"></span>
                    <p class="text-gray-500 text-sm mt-1">역할에 대한 설명이나 용도를 입력하세요. (최대 1000자)</p>
                </div>

                <div class="flex justify-between items-center mt-6">
                    <a asp-action="Index" asp-route-page="@returnPage" asp-route-pageSize="@returnPageSize"
                        asp-route-searchTerm="@returnSearchTerm" asp-route-searchFields="@returnSearchFields"
                        class="px-4 py-2 bg-gray-200 text-gray-700 rounded hover:bg-gray-300 transition-colors">취소</a>
                    <button type="submit"
                        class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 transition-colors">
                        <i class="bi bi-check2 mr-1"></i> 저장
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
