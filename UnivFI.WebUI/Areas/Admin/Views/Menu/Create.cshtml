@model UnivFI.WebUI.Areas.Admin.ViewModels.Menu.MenuViewModel

@{
    ViewData["Title"] = "메뉴 생성";
    var roles = ViewBag.Roles as List<SelectListItem>;
    var parentMenus = ViewBag.ParentMenus as List<SelectListItem>;
    var returnPage = ViewBag.ReturnPage ?? 1;
    var returnPageSize = ViewBag.ReturnPageSize ?? 10;
    var returnSearchTerm = ViewBag.ReturnSearchTerm as string ?? string.Empty;
    var returnSearchFields = ViewBag.ReturnSearchFields as string ?? string.Empty;
}

<div class="container mx-auto px-4 py-6">
    <h1 class="text-2xl font-bold mb-4">새 메뉴 생성</h1>
    <div class="h-0.5 bg-gray-200 w-full mb-6"></div>

    <div class="max-w-6xl mx-auto">
        <form asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="text-red-500 mb-4"></div>

            <div class="bg-white rounded-lg shadow-md overflow-hidden">
                <div class="bg-gray-50 px-6 py-4 border-b">
                    <h2 class="font-semibold text-lg">메뉴 정보</h2>
                </div>
                <div class="p-6">
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div>
                            <div class="mb-4">
                                <label asp-for="ParentId" class="block text-gray-700 font-medium mb-2">상위 메뉴</label>
                                <select asp-for="ParentId"
                                    class="w-full px-4 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-500"
                                    asp-items="parentMenus">
                                </select>
                                <span asp-validation-for="ParentId" class="text-red-500 text-sm"></span>
                                <p class="text-gray-500 text-sm mt-1">이 메뉴의 상위 메뉴를 선택하세요. 최상위 메뉴는 '상위 메뉴 없음'을 선택하세요.</p>
                            </div>

                            <div class="mb-4">
                                <label asp-for="MenuOrder" class="block text-gray-700 font-medium mb-2">메뉴 순서</label>
                                <input asp-for="MenuOrder"
                                    class="w-full px-4 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-500"
                                    type="number" min="0" />
                                <span asp-validation-for="MenuOrder" class="text-red-500 text-sm"></span>
                                <p class="text-gray-500 text-sm mt-1">메뉴가 표시될 순서를 입력하세요. 작은 숫자가 먼저 표시됩니다.</p>
                            </div>

                            <div class="mb-4">
                                <label asp-for="MenuKey" class="block text-gray-700 font-medium mb-2">메뉴 키</label>
                                <input asp-for="MenuKey"
                                    class="w-full px-4 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-500" />
                                <span asp-validation-for="MenuKey" class="text-red-500 text-sm"></span>
                                <p class="text-gray-500 text-sm mt-1">메뉴를 식별하는 고유 키를 입력하세요.</p>
                            </div>

                            <div class="mb-4">
                                <label asp-for="Title" class="block text-gray-700 font-medium mb-2">메뉴 제목</label>
                                <input asp-for="Title"
                                    class="w-full px-4 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-500" />
                                <span asp-validation-for="Title" class="text-red-500 text-sm"></span>
                                <p class="text-gray-500 text-sm mt-1">메뉴에 표시될 제목을 입력하세요.</p>
                            </div>

                            <div class="mb-4">
                                <label asp-for="Url" class="block text-gray-700 font-medium mb-2">URL</label>
                                <input asp-for="Url"
                                    class="w-full px-4 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-500" />
                                <span asp-validation-for="Url" class="text-red-500 text-sm"></span>
                                <p class="text-gray-500 text-sm mt-1">메뉴 클릭 시 이동할 URL을 입력하세요.</p>
                            </div>
                        </div>

                        <div>
                            <div class="mb-4">
                                <label asp-for="Levels" class="block text-gray-700 font-medium mb-2">메뉴 레벨</label>
                                <input asp-for="Levels"
                                    class="w-full px-4 py-2 border rounded focus:outline-none focus:ring-2 focus:ring-blue-500"
                                    type="number" min="0" />
                                <span asp-validation-for="Levels" class="text-red-500 text-sm"></span>
                                <p class="text-gray-500 text-sm mt-1">메뉴의 깊이 레벨을 입력하세요. 최상위 메뉴는 0이나 1로 설정하세요.</p>
                            </div>

                            <div class="mb-4">
                                <label asp-for="UseNewIcon" class="block text-gray-700 font-medium mb-2">새 아이콘
                                    사용</label>
                                <div class="mt-2 flex space-x-4">
                                    <div class="flex items-center">
                                        <input asp-for="UseNewIcon" type="radio" value="true" id="useNewIcon_yes"
                                            class="h-4 w-4 text-blue-600 focus:ring-blue-500" />
                                        <label for="useNewIcon_yes" class="ml-2 text-gray-700">사용</label>
                                    </div>
                                    <div class="flex items-center">
                                        <input asp-for="UseNewIcon" type="radio" value="false" id="useNewIcon_no"
                                            class="h-4 w-4 text-blue-600 focus:ring-blue-500" />
                                        <label for="useNewIcon_no" class="ml-2 text-gray-700">사용 안함</label>
                                    </div>
                                </div>
                                <span asp-validation-for="UseNewIcon" class="text-red-500 text-sm"></span>
                                <p class="text-gray-500 text-sm mt-1">이 메뉴에 새 아이콘을 표시할지 선택하세요.</p>
                            </div>

                            <div class="mb-4">
                                <label class="block text-gray-700 font-medium mb-2">역할 할당</label>
                                <div class="border rounded overflow-hidden">
                                    <div class="max-h-60 overflow-y-auto p-4">
                                        @if (roles != null && roles.Any())
                                        {
                                            @foreach (var role in roles)
                                            {
                                                <div class="flex items-center mb-2">
                                                    <input type="checkbox" name="selectedRoles" value="@role.Value"
                                                        id="role_@role.Value" class="h-4 w-4 text-blue-600 focus:ring-blue-500"
                                                        @(role.Selected ? "checked" : "") />
                                                    <label for="role_@role.Value" class="ml-2 text-gray-700">@role.Text</label>
                                                </div>
                                            }
                                        }
                                        else
                                        {
                                            <div class="bg-yellow-50 border-l-4 border-yellow-400 p-4 text-yellow-700">
                                                사용 가능한 역할이 없습니다.
                                            </div>
                                        }
                                    </div>
                                </div>
                                <p class="text-gray-500 text-sm mt-1">이 메뉴에 접근할 수 있는 역할을 선택하세요.</p>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="bg-gray-50 px-6 py-4 border-t flex justify-between">
                    <a asp-action="Index" asp-route-page="@returnPage" asp-route-pageSize="@returnPageSize"
                        asp-route-searchTerm="@returnSearchTerm" asp-route-searchFields="@returnSearchFields"
                        class="px-4 py-2 bg-gray-200 text-gray-700 rounded hover:bg-gray-300 transition-colors">
                        <i class="fas fa-arrow-left mr-1"></i> 취소
                    </a>
                    <button type="submit"
                        class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 transition-colors">
                        <i class="fas fa-save mr-1"></i> 저장
                    </button>
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}